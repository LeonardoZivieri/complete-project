{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "concurrently --raw \"tsc -w\" \"dotenv -e ../../.env -- func start --port=3030\"",
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@h4ad/serverless-adapter": "^2.14.0",
    "@microsoft/teamsfx": "^2.1.0"
  },
  "devDependencies": {
    "@azure/functions": "^3.0.0",
    "@types/node": "16.x",
    "typescript": "^4.0.0"
  }
}
